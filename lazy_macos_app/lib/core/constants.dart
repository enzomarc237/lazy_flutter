import 'package:flutter/material.dart';

// Define window sizes for different views
const Size kCommandCenterSize = Size(700, 400);
const Size kHistoryViewSize = Size(800, 600);
const Size kSettingsViewSize = Size(600, 500);
